version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: jarvis_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: jarvis_crm
      POSTGRES_USER: jarvis_user
      POSTGRES_PASSWORD: jarvis_secure_password_2026
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jarvis_user -d jarvis_crm"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: jarvis_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: jarvis_api
    restart: unless-stopped
    environment:
      - ENVIRONMENT=production
      - DEBUG=false
      - DATABASE_URL=postgresql://jarvis_user:jarvis_secure_password_2026@postgres:5432/jarvis_crm
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=production-jarvis-secret-key-change-in-env-32-chars
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "8000:8000"
    volumes:
      - storage_uploads:/app/storage_uploads

  worker:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: jarvis_worker
    restart: unless-stopped
    command: celery -A app.worker.celery_app worker --loglevel=info
    environment:
      - ENVIRONMENT=production
      - DATABASE_URL=postgresql://jarvis_user:jarvis_secure_password_2026@postgres:5432/jarvis_crm
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=production-jarvis-secret-key-change-in-env-32-chars
    depends_on:
      backend:
        condition: service_started
      redis:
        condition: service_healthy
    volumes:
      - storage_uploads:/app/storage_uploads

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: jarvis_frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend

  beat:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: jarvis_beat
    restart: unless-stopped
    command: celery -A app.worker.celery_app beat --loglevel=info
    environment:
      - ENVIRONMENT=production
      - DATABASE_URL=postgresql://jarvis_user:jarvis_secure_password_2026@postgres:5432/jarvis_crm
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=production-jarvis-secret-key-change-in-env-32-chars
    depends_on:
      redis:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
  storage_uploads:
